using UnityEngine;

public class TankSpawner : MonoBehaviour {
    /*[SerializeField]
    private DrivableGameUnit drivableGameUnitPrefab;
    [SerializeField]
    private float spawnTime;
    [SerializeField]
    private int maxSpawnCount;

    private float timer = 0f;
    private int spawnCount = 0;

    private void Update() {
        if(spawnCount >= maxSpawnCount) return;
        timer += Time.deltaTime;
        if(timer >= spawnTime) {
            Instantiate(drivableGameUnitPrefab,transform.position,Quaternion.identity);
            timer = 0f;
            spawnCount += 1;
        }
    }*/
}